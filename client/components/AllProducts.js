import React from "react";
//import {connect} from 'react-redux'

export const AllProducts = () => {
  return (
    <div>
      <h3>All Products Component</h3>
    </div>
  );
};

export default AllProducts; //comment if uncommenting below
//export default connect(mapState)(AllProducts) //uncomment when mapState and/or mapDispatch added
