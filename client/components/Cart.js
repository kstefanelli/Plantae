import React from "react";
//import {connect} from 'react-redux'

export const Cart = () => {
  return (
    <div>
      <h3>Cart Component</h3>
    </div>
  );
};

export default Cart; //comment if uncommenting below
//export default connect(mapState)(Cart) //uncomment when mapState and/or mapDispatch added
